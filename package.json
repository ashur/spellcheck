{
  "name": "spellcheck",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "eleventy && npm run build:js",
    "build:js": "esbuild src/static/scripts/bookmarklet.mjs --bundle --outfile=dist/bookmarklet.js",
    "dev": "onchange -i 'src/**/*.mjs' 'test/**/*.mjs' -- npm run test",
    "serve": "eleventy --serve",
    "test": "mocha test/**/*.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.3.4",
    "eslint": "^8.5.0",
    "mocha": "^9.1.3",
    "netlify-cli": "^8.4.2",
    "onchange": "^7.1.0"
  },
  "dependencies": {
    "@11ty/eleventy": "^0.12.1",
    "esbuild": "^0.14.8",
    "rollup": "^2.62.0"
  }
}
